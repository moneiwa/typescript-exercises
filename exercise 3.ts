//1

function addNumbers(a: number, b: number): number {
    return a + b;
}

//2
function fixedNumber(): number {
    return 12;
}


//3

function modifyString(s: string, toUpper: boolean = false): string {
    return toUpper ? s.toUpperCase() : s.toLowerCase();
}

